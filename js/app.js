import{login,ensureDefaultUsers,logout,requireSession}from"./auth/auth.js";import{ROLE_LABELS,FORM_ACCESS}from"./auth/roles.js";import{seedMasterData}from"./db/seed.js";import{renderNotificationBell}from"./notifications/notifications.js";
export async function bootLogin(){await ensureDefaultUsers();await seedMasterData();const form=document.querySelector("#login-form");const msg=document.querySelector("#login-message");form.addEventListener("submit",async(e)=>{e.preventDefault();try{await login(form.username.value,form.password.value);window.location.href="dashboard.html"}catch(err){msg.textContent=err.message;msg.className="notice status-fail"}})}
export async function bootShell(){const session=await requireSession();if(!session)return;document.querySelector("[data-user]").textContent=`${session.fullName} - ${ROLE_LABELS[session.role]}`;document.querySelector("[data-logout]")?.addEventListener("click",logout);document.querySelectorAll("[data-nav-form]").forEach(a=>{if(!FORM_ACCESS[a.dataset.navForm]?.includes(session.role)){a.classList.add("nav-disabled");a.setAttribute("aria-disabled","true");a.title="هذا القسم ظاهر للمراجعة، لكن دورك لا يملك صلاحية فتحه";a.addEventListener("click",(event)=>event.preventDefault())}});await renderNotificationBell(session.role);return session}

