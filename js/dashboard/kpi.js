import{getAll}from"../db/db-service.js";const stores=["InProcessControl","DailyQualityReport","ProofingTemperature","MetalDetectorCheck","SieveControl","SensoryEvaluation","NonConformingProduct","CleaningLog","GHPCheck","ReleasePermit","AdditivesControl"];export async function collectKpis(){const data={};for(const s of stores)data[s]=await getAll(s);const all=Object.values(data).flat();const today=new Date().toISOString().slice(0,10);const todayRows=all.filter(r=>String(r.reportDate||r.createdAt).slice(0,10)===today);const fail=all.filter(r=>r.status==="غير مطابق"||r.conformStatus==="غير مطابق"||r.matrixResult==="مرفوض");const sensory=data.SensoryEvaluation.filter(r=>String(r.reportDate).slice(0,10)===today);return{today:todayRows.length,week:all.length,month:all.length,conform:all.length?Math.round(((all.length-fail.length)/all.length)*100):100,openFail:fail.length,releaseToday:data.ReleasePermit.filter(r=>String(r.reportDate).slice(0,10)===today&&r.decision==="تم الإفراج").length,sensoryAvg:sensory.length?(sensory.reduce((a,b)=>a+Number(b.matrixScore||0),0)/sensory.length).toFixed(1):"-",data};}
