import{bootShell}from"../app.js";import{collectKpis}from"./kpi.js";const labels={DailyQualityReport:"تقرير الجودة",ProofingTemperature:"الحرارة",MetalDetectorCheck:"كاشف المعادن",AdditivesControl:"المضافات",CleaningLog:"النظافة",GHPCheck:"GHP"};const session=await bootShell();if(session){const k=await collectKpis();document.querySelector("[data-kpi=today]").textContent=k.today;document.querySelector("[data-kpi=conform]").textContent=`${k.conform}%`;document.querySelector("[data-kpi=openFail]").textContent=k.openFail;document.querySelector("[data-kpi=release]").textContent=k.releaseToday;document.querySelector("[data-kpi=sensory]").textContent=k.sensoryAvg;const bars=document.querySelector("[data-bars]");bars.innerHTML=Object.entries(labels).map(([store,label])=>{const total=k.data[store]?.length||0;const pct=Math.min(100,total*12);return`<div class="bar-row"><span>${label}</span><div class="bar"><span style="width:${pct}%"></span></div><strong>${total}</strong></div>`}).join("");const ctx=document.querySelector("#mainChart");if(window.Chart&&ctx){new Chart(ctx,{type:"bar",data:{labels:Object.values(labels),datasets:[{label:"عدد السجلات",data:Object.keys(labels).map(s=>k.data[s]?.length||0),backgroundColor:"#c9a24b"}]},options:{responsive:true,plugins:{legend:{display:false}}}})}}
